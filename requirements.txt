# Core
numpy>=1.24.0
pandas>=1.5.0
scikit-learn>=1.3.0
scipy>=1.10.0
lightgbm>=4.0.0
tqdm>=4.65.0
nltk>=3.8.0

# Deep learning
# torch is installed separately — get the build matching your CUDA version:
# https://pytorch.org/get-started/locally/
transformers>=4.49.0
accelerate>=1.4.0
datasets>=2.0.0

# Sentence embeddings for SBERT coherence features
# Falls back to TF-IDF cosine similarity if not installed
sentence-transformers>=2.2.0

# Extra-data collection (Track A/B only)
# groq>=0.9.0
# anthropic>=0.25.0
# requests>=2.31.0
# beautifulsoup4>=4.12.0

# Optional: flash attention for faster ModernBERT inference
# flash-attn  (built inside the Dockerfile)